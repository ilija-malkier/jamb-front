import { UpdatePlayerRequest } from './update-player-request';

describe('UpdatePlayerRequest', () => {
  it('should create an instance', () => {
    expect(new UpdatePlayerRequest()).toBeTruthy();
  });
});
