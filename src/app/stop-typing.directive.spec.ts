import { StopTypingDirective } from './stop-typing.directive';

describe('StopTypingDirective', () => {
  it('should create an instance', () => {
    const directive = new StopTypingDirective();
    expect(directive).toBeTruthy();
  });
});
